name: Keep Alive
on:
  schedule:
    - cron: '*/14 * * * *' # Runs every 14 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: curl -i https://portfolio-backend-fsqx.onrender.com/actuator/health